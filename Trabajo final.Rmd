---
title: 'Análisis de los determinantes salariales en el mercado moderno: un estudio
  estadístico.'
author: 'Vianney Becerra C.I: 31.375.207 Santiago lópez López C.I: 30.330.479'
date: "`r Sys.Date()`"
output: pdf_document
toc: true
toc_depth: 2
fontsize: 12pt
number_sections: true
mainfont: "Times new Roman"
indent: true
header-includes:
  header-includes:
   - \usepackage{indentfirst}
   - \setlength{\parindent}{2em}
   - \setlength{\parskip}{1em}
   - \linespread{1.3}
   - \usepackage{fancyhdr}
   - \pagestyle{fancy}
   - \fancyhf{} # Limpia todos los encabezados y pies de página
   - \fancyfoot[R]{\thepage} # Pone el número de página a la derecha (Right)
   - \renewcommand{\headrulewidth}{0pt} # Quita la línea horizontal de arriba
---
\newpage

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

## R Markdown

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
